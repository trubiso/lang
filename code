void main() {
	let x = add(2, 3);

	T add<T>(T a, T b) => a + b;

	i32 a() {
		i32 b() {
			i32 c() {

			}
		}
	}

	let y = add<i32>(2, 3);

	let q = 10;
	let k = q + 3;
	let q = 11;
	let k = q - 3;
}